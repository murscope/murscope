# fixture for: all_rules_have_checks
# shape: a rule heading with neither a matching script nor an Enforcement line
# The body below is base64. Fixtures are data, never live source:
#   a fixture holding real violating code would trip the other checks,
#   and the fix for that is always an exempt directory - which is where
#   every hole the audits found began. Decode with:
#     python3 -c "import base64,sys;print(base64.b64decode(''.join(
#       l for l in open(sys.argv[1]) if not l.startswith('#'))).decode())" FILE
IyBDb250cmlidXRpbmcKCiMjIFJ1bGUgOTk6IGJvYXJkcyBuZXZlciBsaWUgYWJvdXQgc3RhbGVu
ZXNzCgpObyBzY3JpcHQsIG5vIEVuZm9yY2VtZW50IGxpbmUuCg==
